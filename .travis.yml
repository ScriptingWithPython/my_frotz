language: python
notifications:
  webhooks:
    - https://salt.kajigga.com/hook/flask-app-pushed
  on_success: always # default: always
  on_failure: always # default: always
  on_start: always   # default: never
  on_cancel: always # default: always
  on_error: always # default: always
script:
  - pytest 
